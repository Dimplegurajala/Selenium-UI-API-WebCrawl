[pytest]
# Enable log capture and set the level
log_cli = true
log_cli_level = INFO


# Format how the logs look in the report
log_format = %(asctime)s [%(levelname)s] %(message)s (%(filename)s:%(lineno)s)
log_date_format = %Y-%m-%d %H:%M:%S